#General system test
"Every line should return Passed"

#Math test
(If (==
	(+ 3 2) # Each of these lines returns 5
	(- 10 5)
	(* 5 1)
	(/ 20 4)
	(% 15 10)
) Passed Fail)

#Building and summing up lists
(If (< (Eval {+ (Set list (Seq 1 5))}) 20) #{+ 1 2 3 4 5}
Passed Fail)

#Mapping loops and product of a list
(If (Eval * (Map list i (!= i 0)))
Passed Fail)

#Filtering list
(If (== (Eval + (Filter list i (== (% i 2) 1))) 9)
Passed Fail)

#General list modifying
(Join (Remove , (Join (Reverse "e,d" s s a P) ,)))

#Step
(Step (Set start Pass) (Join {start ed}))

#Switch statement
(Switch
	{(== 5 3) Fail1}
	{(> 6 10) Fail2}
	{(!= 3 1) Passed}
	{(< 3 0) Fail3}
	{True Fail4}
)

#Test nested enviroments
(If (!=
		yes
		(Env (Step {
			(Set yes no)
			yes
		}))
	)
Passed Fail)

#Basic user-defined function
(If (== ({{x y} (+ x y)} 2 3) 5)
Passed Fail)

#Succesfull recursive function
(If (== ((Set fact {
	{x}
	(If (> x 0)
		(+ x (fact (- x 1)))
		0
	)
}) 4) 10)
Passed Fail)

#Weird formatting test
(If#Testing
1(Join 		 {Pass ed})"Fail"
)
